export default class Referral {
  constructor(
    userID,
    name,
    // joinDate,
    // spentAmount,
  ) {
    this.userID = userID;
    this.name = name;
    // this.joinDate = joinDate;
    // this.spentAmount = spentAmount;
  }
}
